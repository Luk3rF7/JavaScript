const euros = [29, 76, 41, 85, 46, 5];

const soma = euros.reduce((total, valor) => total + valor, 0);
soma // 118,11
// Atualize seu navegador antes de copiar o código em seu console. Já declaramos euros usando const no exemplo anterior. Você não pode declarar novamente o valor de uma variável constante.