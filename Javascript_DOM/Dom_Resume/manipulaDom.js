const btn = document.querySelector('button')

// manipulando Dom

btn.appendChild // adiciona novo filho 
btn.insertBefore // inserin um novo nó antes de  um no existente
btn.removeChild // remove o nó filho
btn.replaceChild // substitui nó filho
btn.cloneNode // clona um nó

// manipulando Eventos

btn.addEventListener // adiciona um evento ouvindo 
btn.removeEventListener // remove um evento ouvindo